<p style="color: green"><%= notice %></p>

<!--button to close sesion-->
<%= link_to 'Logout', destroy_user_session_path, class:"btn btn-outline-danger", 'data-turbo-method': :delete  %>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Show this user", user %>
    </p>
  <% end %>
</div>

<%= link_to "New user", new_user_path %>
